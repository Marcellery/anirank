/**
 * Anime feature
 * -------------
 * Owns everything related to the anime catalogue and per-user watch lists.
 *
 * Will contain (Milestone 3 + 4):
 *   anime.service.ts    — Supabase queries for anime + user_anime tables
 *   useAnime.ts         — hook: searchAnime(), addToList(), updateWatchStatus()
 *   AnimeCard.tsx       — poster + title card (UI)
 *   AnimeSearch.tsx     — search bar + results list (UI)
 */
