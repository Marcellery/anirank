/**
 * Ranking feature
 * ---------------
 * Owns everything related to Elo scores, ranked lists, and share cards.
 *
 * Will contain (Milestone 5):
 *   ranking.service.ts  — Supabase queries for user_rankings table
 *   ranking.store.ts    — Zustand slice: ranked list, optimistic updates
 *   useRankings.ts      — hook: getRankedList(), getTop25()
 *   RankingList.tsx     — scrollable ranked list with Top 25 highlight (UI)
 *   ShareCard.tsx       — Top 10 / Top 25 shareable image card (UI)
 */
