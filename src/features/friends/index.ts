/**
 * Friends feature
 * ---------------
 * Owns everything related to friend relationships, requests, and comparisons.
 *
 * Will contain (Milestone 6):
 *   friends.service.ts  — Supabase queries for friends table
 *   friends.store.ts    — Zustand slice: friend list, pending requests
 *   useFriends.ts       — hook: sendRequest(), acceptRequest(), compareRankings()
 *   FriendList.tsx      — list of accepted friends (UI)
 *   CompareView.tsx     — side-by-side ranking comparison (UI)
 */
