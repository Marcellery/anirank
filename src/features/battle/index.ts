/**
 * Battle feature
 * --------------
 * Owns everything related to head-to-head anime battles.
 *
 * Will contain (Milestone 4):
 *   battle.service.ts   — Supabase queries for comparisons table
 *   battle.store.ts     — Zustand slice: current pair, loading state
 *   useBattle.ts        — hook: pickWinner(), loadNextPair()
 *   BattleCard.tsx      — anime poster + tap/swipe interaction (UI)
 *   BattleArena.tsx     — two-card layout (UI)
 */
