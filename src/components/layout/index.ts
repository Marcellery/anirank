/**
 * Layout wrapper components barrel.
 *
 * Will export (Milestone 3+):
 *   Screen        — SafeAreaView + background colour wrapper
 *   Header        — title bar with optional back button
 *   TabBarIcon    — icon wrapper used by (tabs)/_layout.tsx
 */
