/**
 * App-wide hooks barrel.
 *
 * Only hooks that are used across multiple features live here.
 * Feature-specific hooks (useBattle, useRankings, useFriends, useAnime)
 * live inside their own feature folder.
 */

export { useSession } from './useSession';
